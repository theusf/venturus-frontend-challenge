import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(180, 59, 140)',
    },
    secondary: {
      main: 'rgba(127,43,123,1)',
    },
  },
});

export default theme